classDiagram
    direction LR

    class User {
        +id: BIGINT
        +email: VARCHAR
        +password_hash: VARCHAR
        +created_at: DATETIME
    }

    class Stock {
        +id: BIGINT
        +symbol: VARCHAR
        +company_name: VARCHAR
    }

    class UserStock {
        +id: BIGINT
        +user_id: BIGINT
        +stock_id: BIGINT
        +shares: DECIMAL
        +purchase_price: DECIMAL
        +is_purchased: BOOLEAN
        +created_at: DATETIME
    }

    class StockDailyPrice {
        +id: BIGINT
        +stock_id: BIGINT
        +date: DATE
        +open: DECIMAL
        +high: DECIMAL
        +low: DECIMAL
        +close: DECIMAL
        +volume: BIGINT
        +adj_open: DECIMAL
        +adj_high: DECIMAL
        +adj_low: DECIMAL
        +adj_close: DECIMAL
        +adj_volume: BIGINT
        +div_cash: DECIMAL
        +split_factor: DECIMAL
        +created_at: DATETIME
    }

    User "1" --> "many" UserStock : owns
    Stock "1" --> "many" UserStock : referenced by
    Stock "1" --> "many" StockDailyPrice : has