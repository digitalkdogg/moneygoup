================================================================================
PREDICT_TENSORFLOW.PY - METRICS ANALYSIS
================================================================================

Generated: 2026-02-05
File: predict_tensorflow.py

================================================================================
1. INPUT FEATURES & DATA SOURCES
================================================================================

The predict_tensorflow function accepts the following input data:

  - historical_data_input:        OHLCV (Open, High, Low, Close, Volume) data
  - stock_metrics_input:          Fundamental metrics from stock data
  - news_articles_input:          News articles with sentiment scores
  - external_economic_data_input: External economic indicators


================================================================================
2. TECHNICAL INDICATORS (No Explicit Weights)
================================================================================

All technical indicators are included in the feature set equally. The model
learns their individual importance during training. These are applied to the
stock data using pandas_ta library:

  1. MACD (Moving Average Convergence Divergence)
     - No explicit weight
     
  2. Bollinger Bands (BBands)
     - No explicit weight
     
  3. RSI (Relative Strength Index)
     - No explicit weight
     
  4. Stochastic Oscillator
     - No explicit weight
     
  5. Williams %R (WILLR)
     - No explicit weight
     
  6. ATR (Average True Range)
     - No explicit weight
     
  7. SMA (Simple Moving Average) - 20 period
     - No explicit weight
     
  8. EMA (Exponential Moving Average) - 50 period
     - No explicit weight
     
  9. EMA (Exponential Moving Average) - 200 period
     - No explicit weight


================================================================================
3. VOLATILITY METRICS
================================================================================

  1. Log Returns
     - Calculation: np.log(Close / Close.shift(1))
     - No explicit weight
     
  2. Historical Volatility (30-day rolling)
     - Calculation: log_returns.rolling(window=30).std() * sqrt(252)
     - Annualized using 252 trading days
     - No explicit weight


================================================================================
4. FUNDAMENTAL DATA (Stock Metrics)
================================================================================

  - All items from stock_metrics_input dictionary are included
  - Each metric is preserved as-is (or set to 0 if None)
  - No explicit weights assigned
  - Default value: 0 if not provided


================================================================================
5. EXTERNAL ECONOMIC DATA
================================================================================

  - All items from external_economic_data_input dictionary are included
  - Each metric is preserved as-is (or set to 0 if None)
  - No explicit weights assigned
  - Default value: 0 if not provided


================================================================================
6. NEWS SENTIMENT SCORE
================================================================================

  Name: NewsSentiment
  
  Calculation:
    - Iterates through all news_articles_input
    - Adds +1 for each article with sentiment_score == 3 (positive)
    - Subtracts -1 for each article with sentiment_score == -3 (negative)
    - Ignores articles with sentiment_score != 3 and != -3
    - Final value: tanh(sum / relevant_articles_count)
    - Default: 0 if no relevant articles
  
  Weight: Implicit - included as one feature among all others
  
  Sentiment Mapping:
    - 3:  Positive sentiment  (+1 contribution)
    - -3: Negative sentiment  (-1 contribution)
    - Other: Ignored (0 contribution)
  
  Normalization: tanh function bounds result to [-1, 1] range


================================================================================
7. MODEL ARCHITECTURE & TRAINING
================================================================================

  Neural Network Architecture:
    - Input Layer:  time_step * num_features nodes (20 * number_of_features)
    - Hidden Layer 1: 50 neurons, ReLU activation
    - Hidden Layer 2: 25 neurons, ReLU activation
    - Output Layer: 1 neuron (price prediction)
  
  Training Configuration:
    - Loss Function: mean_squared_error
    - Optimizer: adam
    - Epochs: 10
    - Batch Size: 32
  
  Feature Weighting Strategy: IMPLICIT
    - All features are normalized to [0, 1] using MinMaxScaler
    - The neural network learns the importance of each feature through
      backpropagation during training
    - No explicit weights are manually assigned
    - Relative importance is determined by the model automatically


================================================================================
8. FEATURE SCALING & PREPROCESSING
================================================================================

  Scaling Method: MinMaxScaler
    - Range: [0, 1]
    - Fitted on training data (80% of dataset)
    - Applied to test data and full dataset
  
  Data Processing Steps:
    1. Extract only numerical columns
    2. Normalize all features to [0, 1] range
    3. Create sequences with time_step=20
    4. Reshape to (num_samples, 20 * num_features)
  
  Missing Data Handling:
    - Forward fill (ffill) followed by backward fill (bfill)


================================================================================
9. OUTPUT METRICS
================================================================================

  Prediction Output Contains:
    - ticker: Stock symbol
    - current_price: Last close price from historical data
    - predicted_price: Model's predicted next close price
    - predicted_change: Difference between predicted and current price
    - accuracy_metrics:
        * neural_network.mae:  Mean Absolute Error
        * neural_network.rmse: Root Mean Squared Error


================================================================================
10. METRIC WEIGHTING SUMMARY
================================================================================

  EXPLICIT WEIGHTING: NONE
  ├─ All technical indicators: equally weighted (learned by model)
  ├─ All volatility metrics: equally weighted (learned by model)
  ├─ All fundamental data: equally weighted (learned by model)
  ├─ All external economic data: equally weighted (learned by model)
  └─ News sentiment: equally weighted (learned by model)

  IMPLICIT WEIGHTING: 
    - Applied during model training
    - Neural network determines importance via backpropagation
    - Weights are internal to the neural network layers
    - Not exposed or controlled by the user


================================================================================
11. KEY OBSERVATIONS
================================================================================

  1. No manual feature weighting is implemented. The model treats all input
     features equally after MinMax normalization.

  2. The news sentiment score uses tanh normalization to compress values into
     [-1, 1] range, making it comparable to other normalized features.

  3. The model uses a simple feedforward neural network with relatively few
     layers and neurons (50 -> 25 -> 1), which may limit its capacity to
     learn complex feature interactions.

  4. Training is limited to 10 epochs, which may be insufficient for
     convergence depending on dataset size.

  5. The same 80/20 train/test split is used for all predictions without
     cross-validation.

  6. Feature importance is not explicitly calculated or reported; users cannot
     easily determine which factors most influence the predictions.


================================================================================
END OF ANALYSIS
================================================================================
